# Pinned exactly. An unpinned environment lets a dependency change look like
# a code regression — which happened once in this build's history, when a
# system package install wiped site-packages and the suite went from green to
# ModuleNotFoundError. test_hygiene.py fails if any pin is loosened.
#
# Runtime
pyswisseph==2.10.3.2
Flask==3.1.3
gunicorn==23.0.0
#
# 'Ask about this chart' (v1.1). Optional at runtime: with no
# ANTHROPIC_API_KEY the agent panel explains that it is unconfigured and
# every computed section still works, so a deploy without a key is valid.
anthropic==1.3.0
#
# Test
pytest==9.1.1
#
# The browser gate needs playwright, which is 140 MB the running app never
# imports. It lives in requirements-dev.txt so a deploy does not carry it;
# test_hygiene pins BOTH files.
#
# Build-time only (data/build_cities.py; not imported at runtime):
#   geonamescache==2.0.0
